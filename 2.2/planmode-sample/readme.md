1. 该示例使用plan节点创建任务的计划列表
2. 执行节点第一次判断需要工具调用（根据已知的工具集合自己判断），然后将职责切换为工具节点
3. 工具节点一次性完成工具调用，然后将调用结果写入上下文
4. 再次进入执行节点，此时上下文中添加了工具调用的结果，再次调用LLM，完成计划后，返回结束条件

要点
1. 其实所有的节点，自身都没有跟踪数据或者说数据缓存，整体有上下文对象记录
2. 每个节点识别上下文对象、根据上下文对象的要求执行、执行结果写入上下文对象，然后结束当前节点